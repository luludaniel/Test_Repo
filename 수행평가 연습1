#include <stdio.h>

int main() {
    int dan = 5; // 구구단 단 수
    int i; // 반복용 변수(1~9)

    for (i = 1; i < 10; i++) {
        printf("%d * %d = %d\n", dan, i, dan * i); // dan에 맞는 단수의 구구단 출력
    }

    return 0;
}
